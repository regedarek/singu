namespace :db do
  desc 'Run DB migrations'
  task :migrate => :app do
  end

  desc 'Rollback migration'
  task :rollback => :app do
  end

  desc 'Dump the database schema'
  task :dump => :app do
  end
end
